{
  "name": "sage-care-monitoring",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "artifacts/mobile"
  ],
  "scripts": {
    "dev": "npm --workspace @sage/mobile run dev",
    "typecheck": "npm --workspace @sage/mobile run typecheck",
    "build": "npm --workspace @sage/mobile run build",
    "serve": "npm --workspace @sage/mobile run serve"
  }
}
